# MindVault — Handoff (compatibility pointer)

Not authoritative. The live record of current unfinished work is `state/current.md`.

This file exists only because tooling still references it:
`scripts/healthcheck.sh` requires it and `scripts/search_context.sh` lists it.
Do not write new state here.

To retire: rewire `scripts/healthcheck.sh`, `scripts/search_context.sh`,
`wiki/PI_INTEGRATION.md`, and `wiki/SOLVE.md` to `state/current.md`, then delete this file.
